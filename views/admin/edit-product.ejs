<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/main.css" rel="stylesheet">
    <link href="/css/product.css" rel="stylesheet">
</head>
<body>
    <%- include('../includes/nav.ejs', {isAuthenticated : isAuthenticated, user : username}) %>

    <main>
        <form method="POST" action="/admin/<% if (editing === 'false') { %>add-product<% } else { %>edit-product<% } %>" class="product-form">
            <div class="form-control">
                <label for="title">Title</label>
                <input type="text" name="title" id="title" autofocus required value="<% if (editing === 'false') { %><% } else { %><%= product.title %><% } %>">
            </div>
            <div class="form-control">
                <label for="price">Price</label>
                <input type="number" name="price" id="price" required value="<% if (editing === 'false') { %><% } else { %><%= product.price %><% } %>">
            </div>
            <button type="submit"><% if (editing === 'false') { %>Add product<% } else { %> Update Product <% } %></button>

            <% if (editing === 'true') { %>
                <input type="hidden" value="<%= product.id %>" name="productId">
            <% } %>
        </form>
    </main>

</body>
</html>